import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  imgLogo: {
    width: 200,
    height: 40,
  },
  headerLeftContainer: {
    paddingLeft: 10,
    flex: 1,
    width: '100%',
    alignItems: 'center',
  },
});

export default styles;
